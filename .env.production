# Production build (`npm run build` / Hostinger).
# Public API origin only — safe to commit (Vite embeds VITE_* in the client bundle).
VITE_API_URL=https://ghostwhite-crocodile-760847.hostingersite.com
